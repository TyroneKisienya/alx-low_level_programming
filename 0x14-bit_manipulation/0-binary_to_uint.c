#include "main.h"

/**
 * binary_to_uint - converter
 *
 * @b: var
 *
 * Return: num
 */

unsigned int binary_to_uint(const char *b);
{
	unsigned int num = 0;

	if (*b == NULL)
		return (0);
	else
		return (num);
}
