#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: 0
 */

int main(void)
{
	char c;

	while (c <= 10) 
	{
		putchar(c + 0);
		c++;
	}
/*
 	while (c = 'a' && c < 'h') 
	{
		putchar(c);
		c++;
	}
	*/
	putchar ('\n');
	return (0);
}
