#include "main.h"
/**
 * _abs -entry
 *
 * @i: prints the absolute value of a no
 *
 * Return: positivr or negative
 */
int _abs(int i)
{
	if (i > 0)
	{
		return (i);
	}
	else
	{
		return (-1 * i);
	}
}
